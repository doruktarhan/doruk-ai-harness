# 03-csv-export — feature
> Status: active — at Step 02 (implement)
> Key files: `src/app.py` · `src/test_app.py` · `roadmap.md` (step spine)

CSV export endpoint for the todo-API demo. A 4-step program: design the column schema →
implement the Flask route → test it → ship the PR. Demonstrates `/feature-roadmap` usage
alongside `/handoff`.
