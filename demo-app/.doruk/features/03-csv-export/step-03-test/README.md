# step-03-test
Add 4 pytest cases to `src/test_app.py`: empty store, one-item store, Content-Type header, Content-Disposition header.
**Done when:** all 4 cases pass; `pytest` exits 0 with no warnings.
